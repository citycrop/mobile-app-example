import { StyleSheet } from 'react-native';

const TextStyles = StyleSheet.create({
  subtitle: {
    fontSize: 16,
    color: '#18191F',
  },
  subtitleCenter: {
    fontSize: 16,
    color: '#18191F',
    textAlign: 'center',
  },
});

export default TextStyles;
